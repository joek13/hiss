# hiss playground

AWS CDK project. Defines the Lambda backend for the Hiss playground.

## Commands

```bash
# Build project
$ npm run build
# Deploy to your AWS account
$ npm run cdk deploy
# Fix formatting
$ npm run format
# Fix linter errors
$ npm run lint:fix
```